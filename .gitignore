node_modules
dist
dist-ssr

# Downloaded source video, survey sheets, yt-dlp binary. Regenerate with
# `npm run assets:clips` — the cut clips in public/clips ARE committed.
.cache

# .env holds committed public defaults; only the .local variants are secret.
.env.local
.env.*.local

.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea
*.tsbuildinfo
